package com.mintos.assignment.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // TODO: Add shared beans (e.g., mappers, converters) if needed in the future
}